<h1 class="text-xl font-bold mb-4">WAHA Events (last 200)</h1>

<turbo-frame id="events_frame">
  <ul id="events_list" class="space-y-1 text-sm font-mono">
    <%= render partial: "waha_events/event", collection: @events, as: :event %>
  </ul>
</turbo-frame>

<p class="mt-4"><%= link_to "← Back to chats", chats_path, class: "text-blue-600 hover:underline" %></p>